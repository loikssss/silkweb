[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "silkweb"
version = "0.1.0"
description = "多 Agent 编织框架——把 Agent 像丝线一样编织成网"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
