[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "silkweb"
version = "0.2.0"
description = "Agent 构建 + 多 Agent 编排框架——把 Agent 像丝线一样编织成网"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
